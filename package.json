{
  "name": "devin-mcdonald-site",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "[ -d vault/.quartz ] && npm --prefix vault/.quartz install || true",
    "predev": "[ -n \"$VAULT_PATH\" ] || node vault/scripts/sync-published.mjs",
    "prebuild": "[ -n \"$VAULT_PATH\" ] || node vault/scripts/sync-published.mjs",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "astro": "^5.0.0",
    "figlet": "^1.8.0",
    "gray-matter": "^4.0.3",
    "rehype-slug": "^6.0.0"
  }
}
